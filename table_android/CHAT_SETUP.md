# 🎉 Чат для Flutter приложения — готово!

Я добавил полнофункциональный чат в ваше мобильное приложение. Вот что было создано:

## 📁 Созданные файлы

### Модели данных:
- `lib/models/message.dart` — класс сообщения
- `lib/models/room.dart` — класс комнаты (чата)

### Сервисы:
- `lib/services/local_storage.dart` — локальное хранилище (SharedPreferences):
  - Генерация случайного имени при первом запуске
  - Сохранение user_id и user_name
  - Восстановление данных при следующем запуске

### Экраны:
- `lib/screens/profile_screen.dart` — экран профиля:
  - Просмотр текущего имени и ID
  - Смена имени
  - Генерация нового случайного имени
  
- `lib/screens/chat_list_screen.dart` — список чатов:
  - Все чаты, в которых состоит пользователь
  - Количество участников и последнее сообщение
  - Кнопка "+" для создания нового чата
  
- `lib/screens/new_chat_screen.dart` — создание нового чата:
  - Ввод названия чата
  - Выбор участников из списка зарегистрированных пользователей
  - Создание комнаты и добавление участников
  
- `lib/screens/chat_screen.dart` — экран переписки:
  - Отправка текстовых сообщений
  - Загрузка фото из галереи
  - Загрузка видео из галереи
  - Real-time обновления (новые сообщения появляются автоматически)
  - Отображение имени отправителя и времени

### Обновлены:
- `lib/main.dart` — добавлены кнопки "Чат" и "Профиль" на главный экран
- `pubspec.yaml` — добавлена зависимость `shared_preferences: ^2.2.0`

## 🚀 Что нужно сделать

### 1️⃣ Обновить зависимости (1 раз)
В терминале в папке `table_android/` выполните:
```bash
flutter pub get
```

### 2️⃣ Запустить приложение
```bash
flutter run
```

## ✨ Функционал

### Первый запуск:
1. При первом запуске приложение генерирует:
   - **Случайное имя** (например: `Blue_Tiger_4521`)
   - **Уникальный ID** пользователя (UUID)
2. Эти данные сохраняются локально на устройстве

### Профиль (`lib/screens/profile_screen.dart`):
- Просмотр текущего ID (только для чтения)
- Редактирование имени (сохраняется локально)
- Кнопка "Сгенерировать имя" для нового случайного имени

### Список чатов (`lib/screens/chat_list_screen.dart`):
- Показывает все чаты, в которых вы состоите
- Для каждого чата: название, количество участников, последнее сообщение, время
- Нажимаем на чат → переходим в него
- Кнопка "+" → создаём новый чат

### Создание чата (`lib/screens/new_chat_screen.dart`):
1. Вводим название чата
2. Выбираем участников (список автоматически обновляется при добавлении новых пользователей в таблицу `chat_users`)
3. Нажимаем "Создать чат"
4. Чат создаётся, вы и выбранные участники добавляются в `room_members`

### Чат (`lib/screens/chat_screen.dart`):
- **Текстовые сообщения** — пишем и отправляем
- **Фото** — нажимаем 📷 → выбираем из галереи → загружается в Supabase Storage → появляется в чате
- **Видео** — нажимаем 🎥 → выбираем из галереи → загружается в Supabase Storage → появляется в чате
- **Real-time** — новые сообщения появляются мгновенно (подписка на таблицу `messages`)
- **Для каждого сообщения** — имя отправителя, время, контент

## 🔄 Как работает синхронизация

1. **Первый запуск** → генерируем имя + ID → сохраняем в `SharedPreferences`
2. **Создание чата** → вставляем в `rooms` → добавляем участников в `room_members`
3. **Отправка сообщения** → вставляем в `messages` с room_id, user_id, user_name
4. **Загрузка медиа** → загружаем файл в `documents` bucket → сохраняем URL в `messages`
5. **Real-time** → Supabase отправляет уведомления о новых/обновленных/удаленных сообщениях → UI обновляется автоматически

## 📊 Таблицы в Supabase (уже созданы)

- **`rooms`** — чаты (id, name, created_by, created_at)
- **`room_members`** — участники (room_id, user_id, user_name, joined_at)
- **`messages`** — сообщения (id, room_id, user_id, user_name, text_content, media_type, media_url, file_name, created_at)
- **`chat_users`** — профили пользователей (user_id, user_name, avatar_url, created_at, updated_at)

## 🔐 Безопасность

- **RLS включен** на всех таблицах
- **Только участники комнаты** могут читать её сообщения
- **Только создатель** может удалить чат
- **Только автор** может удалить своё сообщение

## 🎨 Интерфейс

- Текст (мои сообщения) — синий фон, справа
- Текст (сообщения других) — серый фон, слева
- Фото и видео — нажимайте для просмотра в полном размере
- Время каждого сообщения в нижней части пузыря
- Имя отправителя (только для чужих сообщений)

## ⚙️ Если что-то не работает

### Ошибка: "Не найдена таблица chat_users"
→ SQL скрипт для Supabase нужно было выполнить (должен быть выполнен)

### Ошибка: "Permission denied"
→ Проверьте RLS политики в Supabase (обычно это автоматическое создание при `chat_users` или `messages`)

### Сообщения не появляются в real-time
→ Проверьте интернет соединение и что подписка создана правильно (она создана в коде)

### Нет кнопки "Чат" на главном экране
→ Проверьте, что добавлены импорты и кнопка в `main.dart` (она добавлена автоматически)

## 📝 Далее можно добавить

- ⏳ Видео-звонки (Agora, Jitsi, WebRTC)
- ⏳ Аудио-звонки
- ⏳ Индикатор "печатает" (когда кто-то пишет сообщение)
- ⏳ Прочитано/не прочитано
- ⏳ Поиск по сообщениям
- ⏳ Удаление чата
- ⏳ Редактирование сообщений
- ⏳ Реакции на сообщения (эмодзи)

## 🎯 Готово!

Всё готово к запуску. Просто выполните:
```bash
cd table_android
flutter pub get
flutter run
```

Если возникнут вопросы или ошибки — дайте знать! 🚀
